/** Minimum non-empty poll answers required to create or save a poll. */
export const MIN_POLL_OPTIONS = 2;

/** Maximum poll answers a user can add in the composer. */
export const MAX_POLL_OPTIONS = 10;
