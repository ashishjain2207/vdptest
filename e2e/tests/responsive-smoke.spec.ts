import { test } from '../fixtures/test';

test('Critical guest and authenticated journeys remain usable at supported viewport sizes @low', async () => {
  test.skip(
    true,
    'Missing selector in src/: viewport-specific primary-action selectors for all listed pages are not fully available in scoped selector sources',
  );
});
