import { test } from '../fixtures/test';

test('User searches for users, posts, and hashtags @medium', async () => {
  test.skip(
    true,
    'Missing selector in src/: stable result-type containers and seeded result identifiers are not documented in scoped selector sources',
  );
});
