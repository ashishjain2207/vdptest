import { test } from '../fixtures/test';

test('Registered users exchange a private message @medium', async () => {
  test.skip(
    true,
    'Missing selector in src/: messaging selectors are outside E2E_SCOPE.md selector source list',
  );
});
