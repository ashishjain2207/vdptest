import { test } from '../fixtures/test';

test('User views own profile, public profiles, lists, and individual posts @medium', async () => {
  test.skip(
    true,
    'Missing selector in src/: stable profile-owner/list-item identifiers required by this scenario are not provided in scoped selectors',
  );
});
