import { test } from '../fixtures/test';

test('Guest navigates the landing page and public information pages @medium', async () => {
  test.skip(
    true,
    'Missing selector in src/: landing and public-information selectors are not included in E2E_SCOPE.md selector sources',
  );
});
